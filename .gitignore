.Rproj.user
.Rhistory
.RData
.Ruserdata
/.quarto/
staff.qmd
Flag.csv
FlagRemoval.R
TheScript_3L.log
TheScript_4L.log
TheScript_5L.log
TheScript_CS.log
RepoPull.R
RepoPull.log
FlagRemoval.log
InstrumentQC.Rproj
TheScript.R
.Rprofile

**/*.quarto_ipynb

# OS / editor junk
.DS_Store

# Raw QC staging files (transient — copied in, processed into
# data/*/Archive/*.csv, then deleted by TheScript_*.R)
*.fcs
DailyQCReport_*.CSV

# 8-peak sub-project has its own git repo; site only needs the rendered
# docs/ page + diagnostic images under images/8peak_evo/.
8peak_tracking_spectral/

# 6-peak-ultra sub-project (same pattern): site only needs the rendered page +
# diagnostic images under images/6peak_ultra_evo/, not the subproject internals.
6peak Ultra_tracking_spectral/
*.csv.bak

# Copyrighted journal-article PDFs used as local research reference —
# keep the .md bibliography/summaries, never push the full-text PDFs.
Sensitivity Articles/*.pdf
